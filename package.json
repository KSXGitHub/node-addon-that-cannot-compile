{
  "name": "node_addon_that_cannot_compile",
  "version": "0.1.0",
  "description": "Node.js addon that fails to compile",
  "main": "index.js",
  "private": true,
  "dependencies": {
    "bindings": "~1.2.1"
  },
  "scripts": {
    "build": "node-gyp configure && node-gyp build"
  },
  "gypfile": true
}
